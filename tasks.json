[
  {
    "prompt": "Play by making a guess and completing the memory game by selecting row1, column1 and row2, column2 each time. The indexing start from 0",
    "mcp_config": {
      "local": {
        "command": "docker",
        "args": [
          "run",
          "--rm",
          "-i",
          "memory-game-env:0.1.12"
        ]
      }
    },
    "agent_config": {
      "allowed_tools": [
        "act"
      ],
      "append_setup_output": true
    },
    "setup_tool": {
      "name": "setup",
      "arguments": {
        "size": 2
      }
    },
    "evaluate_tool": {
      "name": "evaluate"
    }
  }
]